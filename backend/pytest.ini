[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests (require Docker services)
    property: Property-based tests (Hypothesis)
timeout = 30
